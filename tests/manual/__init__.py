"""Manual test scripts for VQMS.

These are NOT pytest tests — they are standalone scripts that
test real cloud service connections. Run them manually:
    uv run python tests/manual/test_bedrock_connection.py
"""
